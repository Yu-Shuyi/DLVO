C = [0.001 0.01 0.1];
Y = [0.301 0.282 0.319];
K = [1.039e8 3.287e8 1.039e9];

hold on
for i = 1:3
    n = C(i) * 1000;
    k = k(i);
    y1 = Y(i);
    y2 = Y(i);
    h = 0:1e-11:5e-8;
    a = Va(h);
    b = 128 * Vel(h, n, k, y1, y2);
    c = (a + b) / 4.11e-21
    plot(h * 1e10
    